DROP TABLE USER;

CREATE TABLE USER(
	ID INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
	USERID VARCHAR(255),
	PASSWORD VARCHAR(255),
	USERNAME VARCHAR(255),
	PHONENUMBER VARCHAR(255),
	GENDER VARCHAR(255),
	BIRTHDATE VARCHAR(255),
	BUSINESSNUMBER VARCHAR(255)
);

TRUNCATE TABLE USER;

INSERT INTO USER (USERID, PASSWORD, USERNAME, PHONENUMBER, GENDER, BIRTHDATE, BUSINESSNUMBER)
VALUES ('adm', 'adm', 'ADMIN', '010-0000-0000', '?', '19960924', '');

INSERT INTO USER (USERID, PASSWORD, USERNAME, PHONENUMBER, GENDER, BIRTHDATE, BUSINESSNUMBER)
VALUES ('guest@guest.com', 'guestpassword', 'ADMIN', '010-0000-0000', '?', '19960924', '');

DROP TABLE QRROOMINFO;

CREATE TABLE QRROOMINFO(
    ID INT GENERATED BY DEFAULT AS IDENTITY PRIMARY KEY,
    CREATEID INT NOT NULL,
    QRCODE VARCHAR(1000) NOT NULL,
    NAME VARCHAR(100) NOT NULL,
    CONTENT CLOB,
    CREATEAT TIMESTAMP NOT NULL,
    UPDATEAT TIMESTAMP NOT NULL,
    USEYN char(1) DEFAULT 'Y'
);

TRUNCATE TABLE QRROOMINFO;
INSERT INTO QRROOMINFO(CREATEID, QRCODE, NAME, CONTENT, CREATEAT, UPDATEAT) VALUES ('129', 'TEST', 'TEST', 'TESCONTENT', SYSDATE, SYSDATE);